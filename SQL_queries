Display nth row in sql?

SELECT *
FROM employees
ORDER BY employee_id
LIMIT 1 OFFSET n-1;

For example to get 5th query:

SELECT *
FROM employees
ORDER BY employee_id
LIMIT 1 OFFSET 4;
-----------------------------------------------------------------------------------------------------------------------------------------------------------------
