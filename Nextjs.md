1. Whhy nextJS over ReactJS? What extra features does it provide? 
2. Give real examples code wise? 
3. Explain how the file rendering happens in nextJS difference from ReactJS? 
4. Difference between App router and Pages router? 
5. Why don't we make the useEffect async and just the function async? Because we should not return a promise from useEffect, 
6. Create route in App.jsx with Browser router? Working with LAyout and outlet routes (shared layout elements)
7. Using overlay, outlet for opening modal from a posts suppose card on click opens modal on topmusing nested route.
8. loader property in routes, useLoaderData hook in react-router-dom. How does this work?  Check for the code. 
9. Using action function in react router to send data and loader function to fetch data in react-router.  use action, loader property in the path inside the nested routes
10. 






Key features: 
1. Full stack
2. File based Routing
3. Server side rendering (Finished html sent to the client)

NextJS is both frontend and backend, it is full stack. 
To create a route, we create a new Folder suppose awesome -> now this will redirect to the awesome Route.


